{
  "name": "deck-repl",
  "version": "1.0.0",
  "description": "Find a pokemon using the pokedex-repl!",
  "main": "cli.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && npx tsc",
    "start": "node dist/cli.js",
    "dev": "pnpm build && pnpm start",
    "dev:log": "pnpm dev | tee repl.log",
    "test": "pnpm build && vitest --run"
  },
  "keywords": [],
  "author": "Laureline Paris",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.0.10",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
